{
  "name": "buywise-ai",
  "version": "1.0.0",
  "private": true,
  "description": "BuyWise AI - AI Commerce Verification & Decision Agent",
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "build:server": "npm --prefix server run build",
    "build:client": "npm --prefix client run build",
    "build": "npm run build:server && npm run build:client",
    "typecheck": "npm --prefix server run typecheck && npm --prefix client run typecheck",
    "test": "npx vitest run",
    "test:buywise": "npx vitest run tests/smoke.test.ts tests/aliases.test.ts tests/budget.test.ts tests/requirements.test.ts tests/scoring.test.ts tests/decision.test.ts tests/payment.test.ts"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3",
    "vitest": "^1.2.1"
  }
}
